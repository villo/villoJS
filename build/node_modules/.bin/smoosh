#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../smoosh/bin/smoosh" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../smoosh/bin/smoosh" "$@"
  ret=$?
fi
exit $ret
